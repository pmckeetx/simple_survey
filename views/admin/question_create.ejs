<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Simple Survey</title>

    <!-- Bootstrap -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">

    <link href='/stylesheets/style.css' rel='stylesheet'/>

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">Simple Survey</a>
            </div>
            <ul class="nav navbar-nav">
                <li class="active"><a href="/admin">Admin</a></li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <div class="row">
            <div class="xs-col-12">
                <!-- BEGIN CONTENT -->
                <h2>Create Question</h2>

                <% if( errors ) { %>
                    <div class="alert alert-danger" role="alert">
                        <p>Please correct the following errors</p>
                        <ul>
                            <% for( var i = 0; i < errors.length; i++ ) { %>
                            <li><%= errors[i].msg %></li>
                            <% } %>
                        </ul>
                    </div>
                <% } %>
                
                <form method="post" action="/admin/question">
                    <div class="form-group">
                        <label for="surveyName"></label>
                        <input type="text" class="form-control" value="<%= question.questionText %>" id="questionText" name="questionText" placeholder="Enter the text of your question">
                    </div>
                    <button type="submit" class="btn btn-default">Save</button>
                    <a href="/admin/questions" class="btn btn-default btn-danger">Cancel</a>
                </form>
                <!-- END CONTENT -->
            </div>
        </div>
    </div>
</body>
</html>